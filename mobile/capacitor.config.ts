import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.mariano215.scriptglow.companion',
  appName: 'Script Glow',
  webDir: 'dist',
  ios: { contentInset: 'never' },
};
export default config;
