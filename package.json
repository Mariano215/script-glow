{
  "name": "script-glow",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "concurrently -k -n api,web \"node --watch server/index.js\" \"vite\"",
    "start": "node server/index.js",
    "build": "tsc --noEmit && vite build",
    "test": "node --test tests/*.test.*"
  },
  "dependencies": { "express": "^5.1.0", "pdfjs-dist": "5.4.296" },
  "devDependencies": { "@types/node": "^24.0.0", "concurrently": "^9.2.0", "playwright": "^1.55.0", "typescript": "^5.9.0", "vite": "^7.1.0" }
}
